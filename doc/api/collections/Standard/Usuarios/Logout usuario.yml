info:
  name: Logout usuario
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url_backend}}/user/asdfasdfasfdasdfasdfadfasdf"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{session_token}}"

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("session_token", ' ');

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
