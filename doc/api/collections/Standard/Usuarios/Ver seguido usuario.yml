info:
  name: Ver seguido usuario
  type: http
  seq: 11

http:
  method: GET
  url: "{{url_backend}}/user/follow/53b22dfc-0220-4bd2-ae12-ab32d89edf0d/85ac9432-426f-479f-89f2-52cd92e18063"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
