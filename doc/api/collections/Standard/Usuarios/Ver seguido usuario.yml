info:
  name: Ver seguido usuario
  type: http
  seq: 11

http:
  method: GET
  url: "{{url_backend}}/user/follow/6507162c-8344-4dc7-8e40-3f17604c3ef7/9a74c50c-91d2-4ab6-ac04-3675b276e5d2"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
