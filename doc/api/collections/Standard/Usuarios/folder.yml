info:
  name: Usuarios
  type: folder
  seq: 1

request:
  auth: inherit
  scripts:
    - type: after-response
      code: |-
        const sessionToken = res.getBody()?.data?.sessionToken;

        if (sessionToken) {
          bru.setEnvVar("session_token", sessionToken);
        }
