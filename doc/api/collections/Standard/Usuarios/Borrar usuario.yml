info:
  name: Borrar usuario
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{url_backend}}/user"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "password": "Ja.12121212"
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Endpoint privado con identificación
  Borrar el usuario actual
  ## Recibe
  La contraseña antigua en password
