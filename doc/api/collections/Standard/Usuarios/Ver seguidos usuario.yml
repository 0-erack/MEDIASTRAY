info:
  name: Ver seguidos usuario
  type: http
  seq: 13

http:
  method: GET
  url: "{{url_backend}}/user/follow/followingsList/132b034a-d796-420e-b22a-d385b3266597?page=0"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  params:
    - name: page
      value: "0"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Endpoint público
  Ver los usuarios que siguen un usuario
  ## Recibe
  UUID del usuario a consultar y opcionalmente la página donde mirar
  ## Devuelve
  Usuarios que coinciden con la búsqueda
