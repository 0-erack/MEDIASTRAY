info:
  name: Reportar objeto
  type: http
  seq: 19

http:
  method: POST
  url: "{{url_backend}}/report/7dcd7efe-1e76-44e6-9a3f-e7cccb1d33fa"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "type": "game",
        "text": "es malo"
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
