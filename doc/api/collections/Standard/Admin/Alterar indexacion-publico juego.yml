info:
  name: Alterar indexacion/publico juego
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{url_backend}}/admin/alterIndexGame"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "id": "d0dc134c-8959-4d62-8c25-0b72b6d8c7fd",
        "new": false
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Endpoint privado con identificación para administradores
  Altera el parámetro de público de un juego
  ## Recibe
  UUID del juego a alterar y el nuevo valor en new
  ## Devuelve
  Confirmación de que todo ha ido bien o error
