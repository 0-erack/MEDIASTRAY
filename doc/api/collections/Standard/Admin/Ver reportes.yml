info:
  name: Ver reportes
  type: http
  seq: 4

http:
  method: GET
  url: "{{url_backend}}/admin/reports?page=0&id=e695ad8f-05ae-409e-9e35-653907e2639e"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  params:
    - name: page
      value: "0"
      type: query
    - name: id
      value: e695ad8f-05ae-409e-9e35-653907e2639e
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
