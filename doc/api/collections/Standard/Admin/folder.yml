info:
  name: Admin
  type: folder
  seq: 9

request:
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  auth: inherit

docs:
  content: |-
    # Rutas de administración
    Endpoints de moderación y administrador exclusivas para usuarios con permisos elevados
  type: text/markdown
