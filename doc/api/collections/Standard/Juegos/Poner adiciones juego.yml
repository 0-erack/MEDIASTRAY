info:
  name: Poner adiciones juego
  type: http
  seq: 9

http:
  method: PUT
  url: "{{url_backend}}/game/additions/e152f104-9187-4a35-a873-59d5f849aa68"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "additions": [
          
        ]
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
