info:
  name: Juegos
  type: folder
  seq: 7

request:
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  auth: inherit

docs:
  content: |-
    # Rutas de los juegos
    Endpoints de operaciones relacionadas con los juegos
  type: text/markdown
