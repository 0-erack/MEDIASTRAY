info:
  name: 22 Reportar objeto
  type: http
  seq: 21

http:
  method: POST
  url: "{{url_backend}}/report/{{uuid_item1}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "type": "game",
        "text": "es malo"
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Endpoint privado con identificación
  Reportar un objeto
  ## Recibe
  UUID del objeto a bloquear, type para saber que objeto es y text con la información del reporte
  ## Devuelve
  Confirmación de que todo ha ido bien o error
