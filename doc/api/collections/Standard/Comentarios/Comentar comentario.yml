info:
  name: Comentar comentario
  type: http
  seq: 4

http:
  method: POST
  url: "{{url_backend}}/comment/comment/9270afbb-01a8-4620-a3db-198e254f72cf"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "content": "asdfhaspdfh asdhf asdf oalsiudf aosliduf gholiasdufgholi udasjghf asjdgfkasdlfkhjg"
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
