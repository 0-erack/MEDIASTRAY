info:
  name: Comentar comentario
  type: http
  seq: 4

http:
  method: POST
  url: "{{url_backend}}/comment/comment/4d2574a2-8631-49a7-a789-b4d49975c6f3"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
    - name: X-session-token
      value: "{{session_token}}"
    - name: X-auth-api
      value: "{{api_token}}"
  body:
    type: json
    data: |-
      {
        "content": "si"
      }
  auth:
    type: bearer
    token: "{{session_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Ruta privada para crear un usuario
